# nClaw Web Client (Planned)

This directory is reserved for a future web-based nClaw client.

Currently, the **Flutter app** (`../../../app/`) is the primary client for all platforms (iOS, Android, macOS). The Flutter app builds for web as well via `flutter build web`.

No separate web client is planned for v1.0.
